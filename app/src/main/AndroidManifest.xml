<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.crazyma.batuanimlab">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CardFlipActivity">

        </activity>
        <activity android:name=".slot.SlotActivity">

        </activity>
        <activity android:name=".sprinkle.SprinkleActivity">

        </activity>
        <activity android:name=".progress.BatuProgressViewActivity" />
        <activity android:name=".inner_trans.InnerTransitionActivity"
            android:theme="@style/AppTheme.NoStatus.NoNavigation"/>
        <activity android:name=".crop_row.CropRowActivity" >

        </activity>
        <activity android:name=".galaxy.GalaxyActivity">

        </activity>

        <activity android:name=".image_view.ShapeableImageViewActivity">

        </activity>
    </application>

</manifest>